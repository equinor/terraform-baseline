name: ci

on:
  push:
    branches: [main]
    paths:
      - docs/**
      - mkdocs.yml

jobs:
  deploy:
    uses: equinor/ops-actions/.github/workflows/mkdocs-gh-pages.yml@v8.7.2
    with:
      python_version: "3.10"
      requirements: requirements.txt
