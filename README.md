# terraform-baseline

## Development

1. Install Python:

    ```bash
    sudo apt install python3
    ```

1. Install PIP:

    ```bash
    sudo apt install python3-pip
    ```

1. Install MkDocs:

    ```bash
    pip install mkdocs
    ```

1. Run development server:

    ```bash
    mkdocs serve
    ```
